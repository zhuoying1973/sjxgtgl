# 交互日志 (Interaction Journal)

## [2026-03-21 11:08] 结论：必须降级 Python
- **实验结果**: 修改 `ratio_scheme` 类型注解后，SQLAlchemy 依然因 3.14 的 `typing` 模块改动而崩溃。
- **技术定论**: Python 3.14 目前不适合运行生产级别的依赖库（SQLAlchemy, FastAPI）。
- **专家建议**: 
    - 卸载或无视 Python 3.14。
    - 安装 Python 3.12 (64-bit)。
    - 重新运行 `pip install -r requirements.txt`。

## [2026-04-02 18:57] 系统重新启动成功
- **启动环境**: 使用系统 Python 3.12 (跳过损坏的 `.venv`)。
- **验证结果**: `Test-NetConnection` 确认端口 8000 监听正常。
- **交付内容**: [Walkthrough.md](file:///C:/Users/Administrator/.gemini/antigravity/brain/59786b80-9fb9-45c5-950b-8bd7737f7ccb/walkthrough.md) 已生成。
- **当前状态**: 已完成历史启动。

## [2026-06-06 09:55] 服务状态检查
- **状态描述**: 收到系统重启通知，前次启动的后台服务已被终止。
- **当前状态**: 服务已停止，等待用户新指令或启动请求。

## [2026-06-06 09:57] 启动需求专家优化协议确立
- **复习备忘**:
  1. 核心功能：数据库升级(V2)、双向控费提成算法、极简调控台、项目管理优化。
  2. 开发环境：Python 3.12 运行环境，使用系统 Python，跳过损坏的 `.venv`；端口 8000。
  3. 待办任务：制作人极简看板优化、阶梯式工序细化、财务结算导出等。
# 交互日志 (Interaction Journal)

## [2026-03-21 11:08] 结论：必须降级 Python
- **实验结果**: 修改 `ratio_scheme` 类型注解后，SQLAlchemy 依然因 3.14 的 `typing` 模块改动而崩溃。
- **技术定论**: Python 3.14 目前不适合运行生产级别的依赖库（SQLAlchemy, FastAPI）。
- **专家建议**: 
    - 卸载或无视 Python 3.14。
    - 安装 Python 3.12 (64-bit)。
    - 重新运行 `pip install -r requirements.txt`。

## [2026-04-02 18:57] 系统重新启动成功
- **启动环境**: 使用系统 Python 3.12 (跳过损坏的 `.venv`)。
- **验证结果**: `Test-NetConnection` 确认端口 8000 监听正常。
- **交付内容**: [Walkthrough.md](file:///C:/Users/Administrator/.gemini/antigravity/brain/59786b80-9fb9-45c5-950b-8bd7737f7ccb/walkthrough.md) 已生成。
- **当前状态**: 已完成历史启动。

## [2026-06-06 09:55] 服务状态检查
- **状态描述**: 收到系统重启通知，前次启动的后台服务已被终止。
- **当前状态**: 服务已停止，等待用户新指令或启动请求。

## [2026-06-06 09:57] 启动需求专家优化协议确立
- **复习备忘**:
  1. 核心功能：数据库升级(V2)、双向控费提成算法、极简调控台、项目管理优化。
  2. 开发环境：Python 3.12 运行环境，使用系统 Python，跳过损坏的 `.venv`；端口 8000。
  3. 待办任务：制作人极简看板优化、阶梯式工序细化、财务结算导出等。
- **拆解方案**:
  - 【角色任务】：全面复习项目历程后重新启动建筑效果图业务管理系统后端。
  - 【背景信息】：系统环境由于 Python 3.14 兼容性问题已切换为系统 Python 3.12。
  - 【具体说明】：先通过文件复习重要交流（已完成），然后启动服务并使用浏览器验证。
  - 【约束条件】：遵循全中文环境、全过程记录、零基础友好及代码链接规范。

## [2026-06-06 09:59] 功能改进分析优化协议
- **拆解方案**:
  - 【角色任务】：审视系统现有功能，提出具有可行性、能够解决业务痛点的改进建议。
  - 【背景信息】：系统涉及客户、项目、工序、工作项（任务）等，前次重构奠定了 V2 双向控费机制，但依然存在交互繁琐、看板简易及结算导出等待办任务。
  - 【具体说明】：深入梳理 `backend/main.py` 和前端页面，提出包含“制作人极简看板优化”、“提成与结算管理升级”、“工序及参数录入体验”等维度的改进构想。
  - 【约束条件】：避免生僻的编程术语，使用点击链接，全中文描述。

## [2026-06-06 10:05] 系统知识检验与回答
- **拆解方案**:
  - 【角色任务】：准确清晰地回答用户的检验问题，阐述项目定义、业务功能、技术栈及改进建议。
  - **背景信息**: 用户需要确认 AI 是否完全理解了本项目的开发背景 and 代码实现。
  - **具体说明**: 从业务场景（建筑效果图管理）、核心功能（客户/项目/工序提成算法）、技术架构（FastAPI/SQLAlchemy/SQLite）和四个具体改进建议进行说明。
  - **约束条件**: 零基础友好，创建 clickable 文件链接，全中文表述。

## [2026-06-06 10:14] 云服务器可行性价值评估
- **拆解方案**:
  - 【角色任务】：分析并评估系统从局域网向阿里云服务器（云端）迁移的可行性、商业价值及技术改造成本。
  - **背景信息**: 半年前系统被设计为仅在局域网内运行，采用 SQLite 文件数据库，不支持异地办公。
  - **具体说明**: 评估云服务器带来的跨地域访问、数据安全保障，并指出在公网环境下必须面对的安全加固和图片存储（阿里云 OSS 升级）问题。
  - **约束条件**: 零基础友好，创建 clickable 文件链接，全中文表述。

## [2026-06-06 10:19] 混合架构（局域网大文件 + 云端轻量办公）评估
- **拆解方案**:
  - 【角色任务】：根据设计行业的特殊性，设计并评估“局域网大文件存储 + 云服务器轻量办公”的混合架构方案。
  - **背景信息**: 视频、模型、图纸体积过于庞大（几十GB），不适合在公网传输。
  - **具体说明**: 提出大文件存放于公司内网（NAS/共享盘），而云端系统仅处理项目进度、提成计算、小预览图上传审核的混合协作方案，并规划系统为此需要做的调整。
  - **约束条件**: 零基础友好，创建 clickable 文件链接，全中文表述。

## [2026-06-06 10:25] 阿里云服务器部署部署规划
- **拆解方案**:
  - 【角色任务】：将本地的建筑效果图业务管理系统（即梦助手）迁移并部署至 Ubuntu 22.04 LTS 云服务器，实现 24 小时稳定运行及手机跨地域访问。
  - **背景信息**: 用户提供了云服务器 IP (8.141.3.238) 及 root 账户凭证。系统将采用混合架构模式部署。
  - **具体说明**: 制定包含 SSH 环境检查、依赖库安装、代码包传输、Systemd 后台进程守护配置、防火墙与安全组开放等在内的完整迁移路线。
  - **约束条件**: 隐去敏感密码，零基础友好，创建 clickable 链接，操作指令执行前进行中文解释。

## [2026-06-06 10:27] 服务器多项目隔离共存方案评估
- **拆解方案**:
  - 【角色任务】：评估在已有其他运行项目的云服务器上，如何部署本项目以确保彼此互不干扰、互不污染。
  - **背景信息**: 服务器已有其他生产运行项目，需防范 Python 库版本冲突、端口冲突以及文件目录混淆。
  - **具体说明**: 决定采用【Python 专属虚拟环境(venv) + 独立服务守护(Systemd) + 独立目录管理 + 端口避让探测】的最佳实践进行深度隔离。
  - **约束条件**: 零基础友好，创建 clickable 文件链接，全中文表述。

## [2026-06-06 10:29] 阿里云“99元套餐”服务器规格与选型论证
- **拆解方案**:
  - 【角色任务】：结合阿里云99元/年套餐（通常为2核2G内存，3M带宽）的硬件约束，论证混合架构与 venv 沙盒选型的合理性。
  - **背景信息**: 2G 内存是偏紧凑的硬件资源，3M 公网带宽是典型的外网传输瓶颈。
  - **具体说明**: 说明 2G 内存下不宜运行 Docker 或 MySQL，而 `venv` + SQLite（仅消耗约 100MB 内存）非常契合；3M 带宽下限制大图上传，再次论证了“局域网大文件 + 云端轻量预览图”混合架构的必要性。
  - **约束条件**: 零基础友好，创建 clickable 链接。

## [2026-06-06 10:31] 双机双轨同步与手机 APP (PWA) 方案评估
- **拆解方案**:
  - 【角色任务】：评估“有网云端+内网双轨运行，断网内网应急使用”的双机数据同步机制，并为手机端提供免打包的 App 化（PWA）实现方案。
  - **背景信息**: 用户要求在公司断网时局域网仍能正常办公，且需支持手机端 App 级操作。
  - **具体说明**:
    1. 同步方案：设计“云端主库 + 本地定时同步备份/应急录入”的机制，通过 UUID 改造防止主键冲突。
    2. 手机端：不开发高成本原生 App，通过自适应网页设计 + PWA（渐进式 Web 应用）技术，实现“添加到手机桌面”并以无边框 App 模式运行。
  - **约束条件**: 零基础友好，创建 clickable 链接。

## [2026-06-06 10:33] UI 界面风格决策：经典 Windows 办公风格
- **拆解方案**:
  - 【角色任务】：确立界面设计走向，采用类似经典 Windows 应用软件的常规办公风格，摒弃华而不实的多主题和动态视效。
  - **背景信息**: 为应对阿里云 2G 内存及 3M 带宽的物理性能瓶颈，界面应以简洁、加载速度快、低带宽占用、高实用性为主。
  - **具体说明**: 采用经典 Windows 的“菜单栏 + 状态栏 + 经典灰色/纯色扁平表格”布局，大幅减少 JS 特效与多余图像文件，让系统操作逻辑贴近 ERP/办公软件，确保低性能设备和手机浏览器也能秒开。
  - **约束条件**: 零基础友好，创建 clickable 链接。

## [2026-06-06 10:50] 云端部署成功与网络连通性排查
- **部署成果**: 本地代码通过 SFTP 压缩包安全上传至云端并解压；专属 `venv` 依赖库安装成功；后台守护服务 `archviz-manager.service` 已配置并成功拉起，24小时不关机开机自启。
- **验证结论**:
  1. 远程本地访问 `127.0.0.1:8000` 正常响应（确认后台服务已就绪）。
  2. 远程本地系统防火墙 `ufw` 处于 Inactive 关闭状态（排除操作系统拦截）。
  3. 本地公网连接 `8.141.3.238:8000` 被连接重置/拒绝（判定为阿里云安全组未开放 8000 端口）。
- **下一步指引**: 建议用户在阿里云控制台该服务器的“安全组”中添加允许 TCP 8000 端口入站的规则。

## [2026-06-06 10:58] 阿里云安全组协同配置
- **当前进度**: 用户已登录阿里云控制台，正在协助配置安全组规则。
- **下一步行动**: 本地使用 `curl.exe` 持续探测公网 `http://8.141.3.238:8000` 的连通状态，一旦用户在云端保存成功，本地即可第一时间捕捉到成功信号并通知用户。


## [2026-06-06 10:34] 部署启动：连接准备与环境探查
- **用户反馈**: “好的 开始干活吧” - 授权开启部署流程。
- **下一步行动**:
  1. 检查并安装本地运行环境的 `paramiko`（Python SSH 通信工具），用于通过脚本安全、非交互式地连接阿里云服务器。
  2. 运行 SSH 探查脚本，检测服务器上的 Python 版本及已占用端口。

## [2026-06-06 11:12] 安全组引导与外网连通测试
- **拆解方案**:
  - 【角色任务】：手把手指导用户在阿里云控制台的安全组中开放 8000 端口，并测试网络连通性。
  - **背景信息**: 服务已在云端 8000 端口启动，但外网因阿里云默认安全组拦截无法访问，用户对此操作不熟悉，需要白话式指引。
  - **具体说明**: 提供阿里云控制台安全组操作的详细图文/文字步骤，用户完成配置后，在本地通过命令探测 `8.141.3.238:8000` 的连通性。
  - **约束条件**: 零基础友好，全中文表达，操作过程详细记录，并实时同步到交互日志。

## [2026-06-06 11:17] 安全组界面确认
- **当前状态**: 用户发送了安全组规则截图，确认已进入正确的安全组配置页面 `sg-2ze3kiby7b93l3dodec0`。页面显示此前曾为端口 5000 开放过规则（描述为“即梦助手”）。
- **下一步行动**: 指引用户在该页面点击“添加规则”按钮，新建一条针对 8000 端口的允许规则。

## [2026-06-06 11:19] 弹窗具体字段填写指引
- **当前状态**: 用户点击了“添加规则”，弹出了详细的新建规则窗口。
- **下一步行动**: 指引用户逐一在“访问来源”和“访问目的”输入框中输入 `0.0.0.0/0` 和 `8000`，并点击右下角的提交按钮。

## [2026-06-06 11:21] 项目名称厘清与更正
- **重要澄清**: 纠正了命名混淆。服务器 5000 端口上运行的旧项目才是“即梦助手”，而我们本次部署在 8000 端口的是“效果图业务管理系统”。描述内容应更正为“效果图业务管理系统”。
- **下一步行动**: 向用户道歉，纠正指导中的描述文案为“效果图管理”，并等待用户提交。

## [2026-06-06 11:23] 外网成功连通与部署收尾
- **测试结果**: 本地终端执行 `curl.exe -I http://8.141.3.238:8000` 成功收到来自服务器 `uvicorn` 的响应，返回 HTTP 405 (因为使用的是 HEAD 方法，服务端仅允许 GET)，表明 8000 端口安全组放行成功，外部流量已能顺畅访问。
- **下一步行动**: 引导用户使用电脑和手机浏览器访问 `http://8.141.3.238:8000` 进行登录测试，并生成一份系统交付说明。

## [2026-06-06 11:31] 成功登录与“局域网共享路径”解答
- **当前状态**: 用户反馈已成功登录云端系统。用户针对项目创建页面的“局域网共享路径”字段提问，要求解释其使用方法和具体作用。
- **解答策略**: 
  1. 说明该字段是“局域网大文件+云端轻量办公”混合架构的核心桥梁。
  2. 详细列出项目经理（如何填写共享路径）和制作人员（如何复制并一键打开局域网目录）的使用步骤。
  3. 强调其解决 3M 带宽瓶颈、防止云服务器硬盘爆满的关键价值。

## [2026-06-06 11:39] 局域网路径保存验证与一键复制功能设计
- **排查结果**: 远程执行脚本确认云端数据库中 ID 为 1 的项目已完全成功写入了路径 `'\\\\Server\\p\\26年项目\\26.06\\2606001冯立杰长垣远洋铝业'`，且后端返回 303 正常。用户所说“没反应”可能由于：
  1. 表单保存重定向无弹窗提示。
  2. 误以为可以直接点击路径文本在浏览器内打开文件夹，而浏览器因安全沙盒机制限制了直接点击本地协议。
- **优化方案**:
  - 在 [project_detail.html](file:///e:/My_AI_Projects/archviz-biz-manager/backend/templates/project_detail.html) 中为该输入框添加“一键复制”按钮及点击提示。
  - 在 [project_edit.html](file:///e:/My_AI_Projects/archviz-biz-manager/backend/templates/project_edit.html) 中同步做此交互优化。

## [2026-06-06 11:41] 复制功能部署上线与服务重载验证
- **部署状态**: 运行 `deploy.py` 成功将优化后的前端模板传输至云服务器 `/var/www/archviz-biz-manager`。
- **服务状态**: 运行 `ssh_restart.py` 重启远程后台守护服务。进程 `Active: active (running)` 刷新，成功在 `http://0.0.0.0:8000` 重新拉起。
- **验证结果**: 用户可以直接刷新网页，看到局域网共享路径输入框右侧新增了“一键复制路径”按钮。点击按钮即可安全地把路径放入剪切板，并弹出带小白指南的温馨提示。

## [2026-06-06 11:44] 局域网路径使用频率与“中央索引”价值释疑
- **当前状态**: 用户询问平时在公司里直接打开局域网文件夹是否比通过系统更直接，并确认该路径是否只需在创建项目时粘贴一次。
- **答复策略**:
  1. 确认该路径确实只需在“创建项目”时由经理粘贴登记一次，一劳永逸。
  2. 认同平时直接打开文件夹更直接的做法，说明该字段不强求日常每一步都去复制。
  3. 强调其在“异地/手机查档备忘”、“新人接单防找错”、“财务/经理抽查大图对账”等关键场景下作为“中央索引库”的不可替代价值。

## [2026-06-06 11:52] 局域网总路径自动拼接与物理隔离释疑
- **用户诉求**: 希望在系统里设置一次总路径（如 `\\SERVER\p`），创建项目时输入编号和名称后，系统能自动生成并自动在本地局域网服务器创建物理文件夹，省去复制粘贴。
- **方案构想**:
  1. **物理限制释疑**：因为系统部署在阿里云（公网），共享盘在公司内网，云端是无法越过内网物理防线去局域网建文件夹的。
  2. **自动路径拼接算法**：在后端实现根据编号（如 `2605006`）自动拆解出年份（`26年项目`）和月份（`26.05`），结合配置里的总路径 `\\SERVER\p` 自动拼接为 `\\SERVER\p\26年项目\26.05\2605006项目名称` 并保存。
  3. **交互便捷辅助**：在系统里提供“一键复制新建文件夹名称”，或者直接提供一个“一键下载新建文件夹脚本(.bat)”按钮。项目经理在公司电脑上运行该脚本即可一秒在局域网内建好规范的多层文件夹。

## [2026-06-06 11:53] 项目编号/名称修改的联动与防错设计
- **变更逻辑设计**:
  1. **后端数据库联动**：在项目更新路由中，只要“编号”或“名称”发生修改，系统自动基于最新的 `local_path_root` 配置重新计算并拼接出最新的 `local_path` 存入数据库。
  2. **前端重命名向导**：如果路径发生变化，页面上提供醒目的变更前/变更后提示，让用户知晓局域网文件夹需要重命名。
  3. **一键重命名脚本生成**：网页提供“本地重命名批处理脚本”下载，经理在公司运行该脚本可一键将局域网旧文件夹自动改名为新名字，避免路径丢失。

## [2026-06-06 15:15] 项目详情页防重名拦截开发与部署启动
- **执行内容**:
  1. **前端模板改造**：修改了 [project_detail.html](file:///E:/My_AI_Projects/archviz-biz-manager/backend/templates/project_detail.html)，新增重名防冲突弹窗 HTML 块，并在 DOM 初始化完成后拦截“保存基本信息”表单提交，实施 AJAX 查重逻辑。
  2. **部署与排错**：在运行部署时，发现云服务器（Python 3.10）报错 `SyntaxError: f-string expression part cannot include a backslash`。经过两轮日志提取和全局检索，排查并修复了 [backend/main.py](file:///E:/My_AI_Projects/archviz-biz-manager/backend/main.py) 中两处在 f-string 内直接对反斜杠进行操作的兼容性问题（2523行和2716行的 `_join` 路径拼接方法）。
  3. **服务重载与验证**：重新运行 `deploy.py`，云端日志输出 `Uvicorn running on http://0.0.0.0:8000` 正常挂载。本地通过 `curl` 连通性确认服务在公网完全启动成功。项目详情页防重名及自动路径拼装模块已全线打通，进入实机测试。
## [2026-06-06 17:30] 局域网路径及一键建目录脚本下载功能彻底切除
- **执行内容**:
  1. **用户决策**：由于公网网页受限于浏览器安全沙盒限制，无法直接实现点击秒开本地文件夹，经与用户深入沟通，判定直接在局域网内打开项目更直接，决策砍掉“局域网共享路径及脚本下载”功能，轻量化系统交互。
  2. **前端精简**：从 [project_detail.html](file:///E:/My_AI_Projects/archviz-biz-manager/backend/templates/project_detail.html)、[project_edit.html](file:///E:/My_AI_Projects/archviz-biz-manager/backend/templates/project_edit.html) 和 [settings_finance.html](file:///E:/My_AI_Projects/archviz-biz-manager/backend/templates/settings_finance.html) 中彻底移除了共享路径、重命名向导、复制路径以及根路径设置的所有 HTML 块与 JS 事件监听。
  3. **后端裁剪**：在 [backend/main.py](file:///E:/My_AI_Projects/archviz-biz-manager/backend/main.py) 中，彻底删除了 `/download-create-script` 和 `/download-rename-script` 这两个打包下载脚本的接口。并在新增项目和修改保存项目接口中移除了关于局域网共享路径的计算、判断及 session 缓存操作，后端通过编译测试。
  4. **云端部署**：代码修改已就绪，准备重新打包上传并重启云端服务。

## [2026-06-06 17:42] 任务负责人精准区分逻辑专家优化协议确立
- **拆解方案**:
  - 【角色任务】：解决多名制作人在指派与编辑任务时人名和岗位混淆的痛点，确保系统能够清晰展示“具体是哪一个后期人员”。
  - **背景信息**: 本地代码已完成了对“姓名（专业岗位/技能）”的改造，并在人员管理中引入了自定义的“专业岗位”属性，但尚未部署至云端，导致服务器依然运行着旧版的简易版界面。
  - **具体说明**: 
    1. 启动本地 `deploy.py` 上传代码至阿里云服务器。
    2. 运行重启脚本 `ssh_restart.py` 重建服务器后台进程。
    3. 引导用户前往“人员管理”页面对相同的后期制作人员设置专属专业岗位名称（例如“后期-老张”、“后期-小刘”）。
    4. 实机测试验证编辑任务时，下拉列表是否能根据人员岗位准确分类与展示。
  - **约束条件**: 零基础友好，全中文表达，操作过程详细记录，并实时同步到交互日志。

## [2026-06-06 18:09] 修复数据库迁移启动崩溃与重新部署上线
- **问题排查**:
  - 重启云端时，系统抛出 `sqlite3.OperationalError: no such column: users.skills` 错误崩溃。
  - **报错原因**：之前将迁移逻辑放在了路由方法 `people_page` 中，但在程序启动的 `on_startup` 生命钩子中，系统会首先通过 `ensure_admin_password_hash` 去查询 `User` 表。由于查询时 users 表尚无该字段，导致在进入路由前服务就崩溃退出。
- **解决方案**:
  - 在 [backend/main.py](file:///E:/My_AI_Projects/archviz-biz-manager/backend/main.py) 中，定义了专用的 `migrate_users_skills_if_needed(db)` 安全迁移函数，利用 `PRAGMA table_info` 来无损检查。
  - 将该函数的执行时机调整到 `on_startup` 阶段且位于管理员用户查询之前。
  - 彻底清理了 `/people` 页面路由中陈旧的就地检测。
- **部署与验证**:
  - 本地运行 `python -m py_compile backend/main.py` 编译正常。
  - 重新部署后，通过单独运行 `ssh_restart.py` 验证，云端后台守护服务成功重启，PID 为 44376，状态为 `Active: active (running)`，且打印了 `Application startup complete`，证明数据库初始化迁移和项目改动已全部上线。

## [2026-06-06 18:22] 修复 my-performance 页面 date_only 过滤器 500 报错与更新部署
- **问题排查**:
  - 用户反馈访问 `http://8.141.3.238:8000/my-performance` 页面时，抛出 `TemplateAssertionError: No filter named 'date_only'` 的 500 渲染崩溃。
  - **报错原因**：`my_performance.html` 模板中试图调用 `date_only` 日期转换过滤器，但该自定义过滤器此前从未在 `backend/main.py` 中进行过声明和注册。
- **解决方案**:
  - 在 [backend/main.py](file:///E:/My_AI_Projects/archviz-biz-manager/backend/main.py) 中新增了 `_format_date_only` 函数实现（剥离时间，仅格式化为 `YYYY-MM-DD` 字符）。
  - 在 Jinja2 模板过滤注册表中注册了 `templates.env.filters["date_only"] = _format_date_only`。
- **部署与验证**:
  - 本地静态编译成功。
  - 重新运行 `deploy.py` 上传最新改动至服务器，并通过 `ssh_restart.py` 成功重启了后台进程（PID: 44636）。测试返回状态正常，`Application startup complete` 已打印，`date_only` 自定义过滤器已被正确注册。

## [2026-06-06 18:34] 释疑管理系统与局域网的彻底物理脱钩
- **用户提问**: 目前是不是管理系统和局域网没有任何关系？
- **解答确认**:
  - **网络与技术层面**：完全没有任何物理连通或网络依赖。管理系统运行于阿里云服务器上，局域网共享盘依然存放在公司内部，两者相互物理隔离。
  - **业务逻辑协作**：
    1. **云端系统**：只负责处理项目登记、状态跟踪、任务指派、工序提成等轻量化的信息管理，支持手机和异地随时随地访问。
    2. **局域网共享盘**：依然由员工在公司内网里手动访问 and 创建项目大文件（模型、贴图、源文件等），不经过公网，确保大文件读取的绝对高速与内网安全性。

## [2026-06-06 18:46] 上传完工凭证图片大小收紧限制专家优化协议确立
- **拆解方案**:
  - 【角色任务】：解决设计师提交完工任务时上传预览图片凭证的需求，且对上传的文件格式进行严格校验，并将图片大小限制从原本的 5MB 收紧至 1MB。
  - **背景信息**: 系统在 `task_detail.html` 的前端以及后端中，已经初步实现了工作凭证（图片上传与删除）的功能，但当前限制是 5MB。
  - **具体说明**: 
    1. 前端模板：修改 [task_detail.html](file:///E:/My_AI_Projects/archviz-biz-manager/backend/templates/task_detail.html) 中提示信息，将 `5MB` 更改为 `1MB`。
    2. 前端脚本：修改 `task_detail.html` 中的 JS 校验，对超过 1MB 的图片进行前端拦截并友好提示。
    3. 后端接口：修改 [backend/main.py](file:///E:/My_AI_Projects/archviz-biz-manager/backend/main.py) 中的 `upload_task_proof` 路由大小限制，将大小判断从 `5MB` 收紧至 `1MB`。
    4. 执行本地编译测试，运行 `deploy.py` 和 `ssh_restart.py` 上线新限制。
  - **约束条件**: 零基础友好，全中文表达，操作过程详细记录，并实时同步到交互日志。

## [2026-06-06 18:53] 云端服务器容量与打开速度评估（1MB vs 5MB）
- **核心算账**:
  - **服务器可用磁盘空间**：按保守估计的 20GB (20,480MB) 剩余可用空间计算。
  - **5MB 方案下**：
    * 极限数量：约 4,096 张图。
    * 按每个项目均用 15 张图：约能存 273 个项目。大约 1-2 年磁盘就会爆满。
    * 打开速度：云服务器 3M 带宽最大下载速度 375KB/s，打开一张 5MB 图需要 **13.3 秒**，体验极其卡顿。
  - **1MB 方案下 (平均单张图压缩后约 500KB)**：
    * 极限数量：约 40,960 张图。
    * 按每个项目均用 15 张图：约能存 2,730 个项目。可以正常使用十几年。
    * 打开速度：在 3M 带宽下打开 500KB 左右的图只需 **1.3 秒**，基本能做到秒开，体验极其流畅。
- **技术决策建议**：极力建议收紧限制至 1MB 以内，节约空间的同时保障外网秒开的访问体验。

## [2026-06-06 18:54] 上传限制从 5MB 正式收紧为 1MB 代码开发与部署
- **执行内容**:
  - **前端修改**：在 [task_detail.html](file:///E:/My_AI_Projects/archviz-biz-manager/backend/templates/task_detail.html) 中将提示信息和 JS 的拦截值从 `5MB` 更改为 `1MB`，并修改了前端 alert 报错提示词。
  - **后端修改**：在 [backend/main.py](file:///E:/My_AI_Projects/archviz-biz-manager/backend/main.py) 的 `upload_task_proof` 路由接口中，将字节长度校验从原先的 `5 * 1024 * 1024` 修改为 `1 * 1024 * 1024` (1MB)，并同步更新返回的 JSON 报错提示。
- **部署与验证**:
  - 本地静态编译成功。
  - 重新运行 `deploy.py` 上传最新改动至服务器，并通过 `ssh_restart.py` 成功重启了后台进程（PID: 44917）。测试返回状态正常，新版 1MB 上传大小控制和文件格式拦截已在云端全部生效。

## [2026-06-06 19:26] 系统更名为“晟景建筑效果图业务管理”专家优化协议确立
- **拆解方案**:
  - 【角色任务】：将系统的品牌名称从原本的“建筑效果图业务管理”变更为“晟景建筑效果图业务管理”。
  - **背景信息**: 经过全局文件扫描，确认系统品牌名称引用位于公共布局模板 `base.html` 的顶部导航栏中。
  - **具体说明**: 
    1. 前端模板：修改 [base.html](file:///E:/My_AI_Projects/archviz-biz-manager/backend/templates/base.html) 中的第 140 行品牌文字。
    2. 执行部署并重启云端服务，完成品牌名称全站更新。
  - **约束条件**: 零基础友好，全中文表达，操作过程详细记录，并实时同步到交互日志。

- **部署与验证**:
  - 本地静态编译成功。
  - 重新运行 `deploy.py` 上传最新改动至服务器，并通过 `ssh_restart.py` 成功重启了后台进程（PID: 45184）。测试返回状态正常，顶部导航栏的品牌名称现已全局变更为“晟景建筑效果图业务管理”。


## [2026-06-07 15:43] 本地初始化 Git 与虚拟环境修复（前置工作）
- **执行内容**:
  1. **Git 仓库初始化**：在项目根目录创建了 `.gitignore`，过滤了虚拟环境、数据库及临时包文件。在本地初始化了 Git 仓库并执行了首次提交（Commit ID: `9e43a2c`），成功为项目代码锁定了“基线备份”。
  2. **虚拟环境路径配置修复**：针对 Windows 运行环境，读取并修改了 `.venv/pyvenv.cfg` 中硬编码的 Linux 路径 `home = /usr/bin`。将其更正为用户本机的实际 Python 安装路径 `C:\Users\xiao_\AppData\Local\Python\pythoncore-3.10-64`。修复后，虚拟环境（包含 FastApi、Uvicorn、SQLAlchemy 等）完美复活。
  3. **服务拉起**：运行本地命令 `.\.venv\Scripts\python.exe -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000` 成功在本地 8000 端口拉起开发测试服务。

## [2026-06-07 15:45] 任务一：个人结算看板优化完工与本地验证
- **执行内容**:
  1. **后端路由改造**：修改了 [backend/main.py](file:///e:/My_AI_Projects/archviz-biz-manager/backend/main.py) 的 `/my-performance` 页面路由。不仅查询已完工结算的提成明细，还增查了当前设计师名下属于“待办”、“进行中”、“审核中”状态的活跃任务数据 (`active_tasks`) 并传递给前端模板。
  2. **前端数据卡片动态化**：修改了 [my_performance.html](file:///e:/My_AI_Projects/archviz-biz-manager/backend/templates/my_performance.html)。通过非零判断，让“完成点数”卡片与“完成张数”卡片仅在数值非零时才在页面上渲染，彻底移除了不相关工种显示为 `0` 的冗余信息。
  3. **集成活跃任务板块**：在 [my_performance.html](file:///e:/My_AI_Projects/archviz-biz-manager/backend/templates/my_performance.html) 页面最上方插入了“我当前的任务”工作台表格，允许设计师在同一页面行内流转任务状态（待办 <-> 进行中 <-> 审核中），并提供了一键前往详情页上传完工预览图凭证的快捷按钮。
  4. **全局导航栏精简**：修改了公共布局模板 [base.html](file:///e:/My_AI_Projects/archviz-biz-manager/backend/templates/base.html)。普通设计师（staff）登录后，原先分离的“我的任务”和“我的业绩”二级导航按钮已被合并为统一的“我的工作台”，直接指向 `/my-performance`，简化了日常操作路径。
- **验证结论**: 本地测试服务运行稳定，控制台与渲染未抛出异常，Jinja2 模板渲染正常，实现了设计师看板的一站式闭环交互。
